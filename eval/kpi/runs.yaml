# Which runs the KPIs count. Only runs of the current brief.json count (checked in code by brief_id).
#
# n8n: only runs of the final workflow (per-company research loop, published 2026-09-23 ~16:20 UTC), because
# earlier versions researched differently (see eval/COMPARISON_LOG.md #4-#9). Every run started at or after
# `since` counts towards reliability; the completed ones listed under `runs` are measured. Each listed run is
# exported with: cd n8n && node scripts/export_run.js <executionId>
n8n:
  since: "2026-09-23T16:23:00Z"
  runs:
    client-muebb850-kyrc43: 35
    client-muebyn9q-em7deg: 37
    client-muecjos5-gzgudf: 38
  superseded:  # completed runs of earlier workflow versions: reported for context, never counted
    client-mudhx5in-ji99rx: "execution 29: before the planner, research and layout fixes"
    client-mue7vd2v-fpmduf: "execution 33: planner and layout fixes, but single-agent research"

# CrewAI: completed full runs of the current brief (research not seeded). Seeded A/B runs are used only for the
# fixed-evidence reproducibility figure.
crewai:
  since: "2026-09-23T17:06:00Z"  # fresh runs on current code; runs of 2026-09-20/21 predate the 2026-09-23 fixes
