# Copy to .env (gitignored) and fill in.
OPENAI_API_KEY=sk-...

# Search keys do NOT go here: they belong to the MCP research server. See ../mcp-server/.env.example
# Google credentials do NOT go here either: they live in crewai/.google/ (see README).
